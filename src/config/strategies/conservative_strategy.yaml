trading_preference:
  risk_level: "low"
  trading_style: "swing"
  max_position_per_symbol: 0.15
  max_total_position: 0.50
  prefer_symbols: ["BTC", "ETH"]
  forbidden_symbols: ["SOL", "DOGE"]

risk_control:
  max_daily_loss: 0.02      # 2%
  max_single_loss: 0.01     # 1%
  stop_loss_percentage: 0.02 # 2%
  take_profit_ratio: 1.5    # 1.5:1
  daily_trade_limit: 5

execution:
  mode: "simulated"
  manual_confirm: true
  auto_trading: true
